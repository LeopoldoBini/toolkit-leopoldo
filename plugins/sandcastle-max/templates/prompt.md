# Smoke test

You are running inside a Sandcastle Docker container with this repo bind-mounted at the working directory.

# Task

1. List the files directly under `docs/` (or another low-risk dir if `docs/` does not exist).
2. Reply with the list as bullet points.
3. Do NOT edit files. Do NOT create commits. Read-only check.

# Done

When finished, end your response with this exact line on its own:

<promise>COMPLETE</promise>
